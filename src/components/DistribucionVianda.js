import React from "react";
import '../assets/styles/CustomInfoApp.css';

function DistribucionVianda() {
  return (
    <div className="donacion-container">
      <h3>Distribución de Viandas</h3>
      <p>Ayuda en la distribución de viandas. Indica la heladera origen, la heladera destino, la cantidad de viandas a mover, el motivo de la distribución, y la fecha en que se realizó.</p>
    </div>
  );
}

export default DistribucionVianda;
